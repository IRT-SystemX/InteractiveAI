export type DateFormat = `${number}-${number}-${number}T${number}:${number}:${number}`
export type DateMillisecondsFormat = `${DateFormat}.${number}`
export type UUID = `${string}-${string}-${string}-${string}-${string}`
