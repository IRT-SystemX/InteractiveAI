apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: web-ui
  labels:
    ingress: tls
  annotations:
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/redirect-entry-point: https
spec:
  rules:
    - host: cab.irtsystemx.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              serviceName: web-ui
              servicePort: 80
